import { PreciomenorFilterPipe } from './preciomenor-filter.pipe';

describe('PreciomenorFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PreciomenorFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
