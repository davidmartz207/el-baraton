import { NombreFilterPipe } from './nombre-filter.pipe';

describe('NombreFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new NombreFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
