import { PreciomayorFilterPipe } from './preciomayor-filter.pipe';

describe('PreciomayorFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PreciomayorFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
