import { QuantityFilterPipe } from './quantity-filter.pipe';

describe('QuantityFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new QuantityFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
